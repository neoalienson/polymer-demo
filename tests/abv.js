define([
  'intern!bdd',
  'intern/chai!expect',
  'app/abv'
], function (bdd, expect, app) {
  bdd.describe('As a BA, I would like to browse behaviors', function () {
  /*
    bdd.xit('should upload behaviors to database', function () {
    });

    bdd.xit('should list 12 behviors on index page', function () {
    });
    */
  });
});
